---
import { Image } from 'astro:assets';
import lampada from '../images/lampada.svg';
---

<section class="hero bg-no-repeat bg-center" style="background-size: 40% auto; backgorund">
    <div class="hero-content fade-up">
        <div class="subtitle text-[1.1rem] font-semibold">
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-orange-400 from-10% via-amber-300 via-40% to-orange-500 to-90% soft-neon-text">CONTABILIDADE ULTRAPASSADA, NUNCA MAIS</span>
        </div>
        <h1 class="text-3xl sm:text-4xl md:text-5xl xl:text-6xl 2xl:text-7xl text-white font-extrabold">
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-orange-400 from-10% via-amber-300 via-40% to-orange-500 to-90% soft-neon-text">Tucont</span>
            a primeira contabilidade <span class="text-transparent bg-clip-text bg-gradient-to-r from-orange-400 from-10% via-amber-300 via-40% to-orange-500 to-90% soft-neon-text">integrativa</span> do Brasil!
        </h1>
        <p class="text-[1.35rem] leading-6 font-semibold text-white">
            Somos a plataforma que vai com você, da ideia ao MVP, da venda a nota fiscal, do CNPJ aos tributos
 <br>Destrave o empreendedorismo, traga ou abra sua empresa e emita notas no mesmo dia.
        </p>
        <div class="flex flex-wrap gap-4">
            <a href="https://rouvbymulgc.typeform.com/to/OSIekGSL?typeform-source=www.tucont.com.br" target="_blank" class="btn-primary">
                Abrir Empresa
            </a>
            <a href="https://rouvbymulgc.typeform.com/to/tgVFjuN4" target="_blank" 
               class="btn-plans text-white font-bold text-base px-8 py-3 rounded-xl min-w-[180px] text-center">
                Migrar para Tucont
            </a>
        </div>
    </div>
    <div class="hero-image fade-up LAMPADA">

    </div>
</section>

<style is:global>
    .hero {
        display: flex;
        align-items: center;
        min-height: 100vh;
        padding: 4rem 7% 8rem 7%;
        margin-top: -2rem; 
        position: relative;
        overflow: hidden;
        backdrop-filter: blur(40px);
        -webkit-backdrop-filter: blur(40px);
    }

    .hero-content {
        max-width: 600px;
        z-index: 2;
        margin-right: 80px;
    }

    .subtitle {
        display: inline-block;
        font-size: 0.9rem;
        text-transform: uppercase;
        letter-spacing: 1px;
        padding: 0.5rem 1rem;
        margin-bottom: 1.5rem;
        border-radius: 8px;
    }

    .hero h1 {
        line-height: 1.1;
        margin-bottom: 1.5rem;
    }

    .hero p {
        margin-bottom: 2.5rem;
        line-height: 1.7;
    }

    .hero-image {
        position: absolute;
        right: 10%;
        top: 50%;
        transform: translateY(-50%);
        width: 50%;
        z-index: 1;
    }

    /* Botão Padrão Reutilizável */
    .btn-primary {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        text-decoration: none !important;
        color: white !important;
        font-size: 1rem;
        font-weight: 500;
        padding: 0.75rem 2rem;
        min-width: 180px;
        height: 48px;
        border-radius: 12px;
        background: linear-gradient(to right, #f59e0b 20%, #ea580c 80%);
        box-shadow: 0 0 8px rgba(255, 165, 0, 0.5),
                    0 0 15px rgba(255, 100, 0, 0.3);
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
        border: none;
        outline: none;
        cursor: pointer;
    }

    .btn-primary:hover, 
    .btn-primary:focus {
        transform: scale(1.05);
        box-shadow: 0 0 12px rgba(255, 195, 0, 0.7),
                    0 0 25px rgba(255, 120, 0, 0.5),
                    0 0 35px rgba(255, 80, 0, 0.3);
        filter: brightness(1.1);
        text-decoration: none !important;
    }

    /* Efeito de brilho interno no hover */
    .btn-primary::before {
        content: '';
        position: absolute;
        top: -50%;
        left: -50%;
        width: 200%;
        height: 200%;
        background: radial-gradient(
            circle, 
            rgba(255, 230, 0, 0.2) 0%, 
            transparent 70%
        );
        transform: scale(0);
        transition: transform 0.5s ease;
        opacity: 0;
    }

    .btn-primary:hover::before {
        transform: scale(1);
        opacity: 1;
    }

    /* Botão Planos - Estilo baseado no dropdown (#5f5f5f) */
     .btn-plans {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        text-decoration: none !important;
        color: white !important;
        font-size: 1rem;
        font-weight: 500;
        padding: 0.75rem 2rem;
        min-width: 180px;
        height: 48px;
        border-radius: 12px;
        background: linear-gradient(to right, #243dbd 20%, #122687 80%);
        box-shadow: 0 0 8px rgba(100, 100, 100, 0.5),
                    0 0 15px rgba(80, 80, 80, 0.3);
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
        border: none;
        outline: none;
        cursor: pointer;
    }

    .btn-plans:hover, 
    .btn-plans:focus {
        transform: scale(1.05);
        box-shadow: 0 0 12px rgba(5, 20, 104, 0.7),
                    0 0 25px rgba(29, 26, 218, 0.5),
                    0 0 35px rgba(4, 7, 196, 0.3);
        filter: brightness(1.1);
        text-decoration: none !important;
        background: linear-gradient(to right, #1905ce 20%, #0a17cc 80%);
    }

    /* Efeito de brilho interno no hover */
    .btn-plans::before {
        content: '';
        position: absolute;
        top: -50%;
        left: -50%;
        width: 200%;
        height: 200%;
        background: radial-gradient(
            circle, 
            rgba(255, 255, 255, 0.15) 0%, 
            transparent 70%
        );
        transform: scale(0);
        transition: transform 0.5s ease;
        opacity: 0;
    }

    .btn-plans:hover::before {
        transform: scale(1);
        opacity: 1;
    }

    /* Efeito de borda sutil no hover */
    .btn-plans::after {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        border-radius: 12px;
        padding: 1px;
        background: linear-gradient(135deg, rgba(150, 150, 150, 0.26),rgba(200, 200, 200, 0.2), rgba(150, 150, 150, 0.4));
        -webkit-mask: 
            linear-gradient(#fff 0 0) content-box, 
            linear-gradient(#fff 0 0);
        -webkit-mask-composite: xor;
        mask-composite: exclude;
        pointer-events: none;
        opacity: 0;
        transition: opacity 0.3s ease;
    }

    .btn-plans:hover::after {
        opacity: 1;
    }

    /* Efeitos Neon Aprimorados */
    .soft-neon-text {
        text-shadow: 0 0 4px rgba(255, 145, 0, 0.4), 
                     0 0 8px rgba(255, 94, 0, 0.3);
    }

    /* Efeito de Lâmpada Neon - Cores mais vibrantes */
    .lampada-neon {
        width: 100%;
        animation: neon-pulse 2.5s ease-in-out infinite;
        filter: 
            drop-shadow(0 0 10px rgba(255, 180, 50, 0.9))
            drop-shadow(0 0 20px rgba(255, 120, 30, 0.8))
            drop-shadow(0 0 30px rgba(255, 80, 10, 0.7));
        transform-origin: center;
    }

    @keyframes neon-pulse {
        0%, 100% {
            filter: 
                drop-shadow(0 0 10px rgba(255, 180, 50, 0.9))
                drop-shadow(0 0 20px rgba(255, 120, 30, 0.8))
                drop-shadow(0 0 30px rgba(255, 80, 10, 0.7));
        }
        50% {
            filter: 
                drop-shadow(0 0 15px rgba(255, 200, 70, 1.0))
                drop-shadow(0 0 25px rgba(255, 140, 40, 0.9))
                drop-shadow(0 0 35px rgba(255, 100, 20, 0.8));
        }
    }

    /* Efeito de brilho mais vibrante e vivo */
    .hero-image::before {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 80%;
        height: 80%;
        background: radial-gradient(
            circle at center, 
            rgba(255, 200, 50, 0.3) 0%,
            rgba(255, 160, 30, 0.25) 30%,
            rgba(255, 120, 10, 0.15) 60%,
            transparent 80%
        );
        border-radius: 50%;
        z-index: -1;
        filter: blur(20px);
        animation: blob-pulse 3s ease infinite alternate;
    }

    @keyframes blob-pulse {
        0% {
            opacity: 0.9;
            transform: translate(-50%, -50%) scale(0.98);
        }
        100% {
            opacity: 1;
            transform: translate(-50%, -50%) scale(1.02);
        }
    }

    /* Remover sublinhado de todos os links */
    a {
        text-decoration: none !important;
    }
</style>